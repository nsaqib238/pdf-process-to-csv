fastapi==0.109.0
uvicorn[standard]>=0.27.0,<0.32
python-multipart==0.0.6
pydantic==2.5.3
pydantic-settings==2.1.0
# PyPDF2 is deprecated; pypdf is the maintained package (PdfReader / PdfWriter API)
pypdf>=4.0,<6
# pypdf also used for splitting large PDFs into chunks for Adobe API
aiofiles==23.2.1

# Adobe PDF Services SDK (for high-quality OCR with Modal hybrid pipeline)
# See: https://github.com/adobe/pdfservices-python-sdk
pdfservices-sdk>=4.0.0
# Adobe SDK dependencies
requests>=2.31.0
defusedxml>=0.7.1
pdfplumber>=0.11

# Table pipeline: pdfplumber + Camelot + Tabula
# Install Java + Ghostscript on the OS first (see README). NumPy 2.x breaks many camelot wheels.
numpy>=1.26,<2
pandas>=2.0
# OpenCV for Camelot lattice (camelot-py 1.x dropped the [cv] extra; install headless explicitly)
opencv-python-headless>=4.8,<5
camelot-py>=0.11.0,<1.0
# Camelot lattice imports this module; without it you get: No module named 'ghostscript'
# You still need the Ghostscript *binary* on PATH (Windows: gswin64c.exe — see README).
ghostscript>=0.7,<0.9
tabula-py>=2.9.0

# Optional: cropped table image OCR (install Tesseract on PATH)
pytesseract>=0.3.10
Pillow>=10.0

# Optional: AI-powered table enhancement (OpenAI GPT-4 Vision)
# Requires OPENAI_API_KEY in .env and enable_ai_table_discovery=true in config
openai>=1.12.0

# Modal.com integration for self-hosted AI table extraction
# Alternative to OpenAI with 99.8% cost savings
modal>=0.63.0
# Already included above for Adobe SDK, but listing here for clarity
# requests>=2.31.0
urllib3>=2.0.7
